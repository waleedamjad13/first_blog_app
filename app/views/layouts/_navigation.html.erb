<nav class="navbar navbar-expand-lg navbar-custom">
  <%= link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Articles", articles_path, id: "colour", class: "nav-link active" %>
        </li>

        <li class="nav-item">
          <%= link_to "Users", users_path, id: "colour", class: "nav-link active" %>
        </li>

        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="colour" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Actions
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "New Article", new_article_path, class: "dropdown-item" %>
              <li><a class="dropdown-item" href="#">Another action</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">Something else here</a></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <form class="d-flex">
        <% if logged_in? %>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0"> 
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" id="colour" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Your Profile <%= "Admin" if current_user.admin? %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li><%= link_to "Edit Your profile", edit_user_path(current_user), class: "dropdown-item" %></li>
                <li><%= link_to "View Your profile", user_path(current_user), class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="#">Something else here</a></li>
              </ul>
            </li>
          </ul>
          <div class="nav-item">
            <%= link_to 'Log Out', logout_path, method: :delete, class: "nav-link", id: "colour" %>
          </div>
        <% else %>
          <div class="nav-item">
            <%= link_to 'Log In', login_path, class: "nav-link", id: "colour" %>
          </div>
          <div class="nav-item">
            <%= link_to 'Sign Up', signup_path, class: "nav-link", id: "colour" %>
          </div>
        <% end %>
      </form>
    </div>
  </div>
</nav>